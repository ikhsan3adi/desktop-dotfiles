general.import = ["~/.config/alacritty/catppuccin-frappe.toml"]

window = { opacity = 0.7, padding = { x = 8, y = 4 } }

font = { normal = { family = "JetBrainsMono Nerd Font Mono" } }

terminal = { shell = "fish" }
